#include <iostream>

using namespace std;

int main() {
	int tmp, total, sum = 0;
	
	for (int i = 0; i < 10; i++) {
		cin >> tmp;
		if (i == 0) {
			total = tmp;
		}
		else {
			sum += tmp;
		}
	}

	cout << total - sum;

	return 0;
}
