#include <iostream>

using namespace std;

int main() {
	int N, tmp;
	cin >> N;
	
	for (int i = N; i > 0; i--) {
		for (int j = 0; j < N-i; j++) {
			cout << " ";
		}
		tmp = (i * 2) - 1;
		for (int j = 0; j < tmp; j++) {
			cout << "*";
		}
		cout << endl;
	}

	return 0;
}
